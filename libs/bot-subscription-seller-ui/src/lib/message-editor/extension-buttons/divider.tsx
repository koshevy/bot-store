import Divider from '@mui/joy/Divider';

import { ExtensionButton } from '../extension-button-abstract';

export const divider: ExtensionButton = {
  render: () => <Divider orientation="vertical" />,
};
